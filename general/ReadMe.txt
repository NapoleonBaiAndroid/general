1.推送,采用个推,应用退到后台时,用APNS(APNS->APPLE PUSH NOTIFICATION SERVICES
).
    1.1程序在前台,收到时应该弹出提示,同样弹出声音提示,让用户选择是否立即进入指定界面
    1.2程序进入后台(或进程终止)此时采用APNS推送,弹出声音提示<不知何故,如果用户操作手机界面,声音会立即终止>,并显示到通知栏中.
        1.2.1 点击通知栏,根据通知类型来到指定的界面
    1.3处理完一条信息,减少一条数据<或者进入界面,就清除提示文字>

